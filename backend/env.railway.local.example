# SmartSchedule backend – local dev using live Railway Postgres
# Copy to backend/.env and replace placeholder secrets before starting the API.

# --- Database (Railway TCP proxy) ---
# Replace <RAILWAY_DB_PASSWORD> with the password from Railway → Variables.
# Railway Postgres enforces TLS when using the TCP proxy, so keep sslmode=require.
# pgbouncer=true keeps connection counts low so we do not exhaust the shared plan.
DATABASE_URL="postgresql://postgres:<RAILWAY_DB_PASSWORD>@yamanote.proxy.rlwy.net:16811/railway?schema=public&sslmode=require&pgbouncer=true&connect_timeout=15"

# Optional: when running prisma migrate you can point the shadow DB at the same
# instance or (preferably) at a disposable database. Commented out by default.
# SHADOW_DATABASE_URL="postgresql://postgres:<RAILWAY_DB_PASSWORD>@yamanote.proxy.rlwy.net:16811/railway_shadow?schema=public&sslmode=require&pgbouncer=true&connect_timeout=15"

# --- API runtime ---
PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:3000"

# --- Auth ---
# Generate long random secrets locally; do not commit real values.
JWT_SECRET="replace-with-long-random-string"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# --- Logging verbosity ---
LOG_LEVEL=debug

# Add any other overrides (Redis, analytics, etc.) below as needed.

