# Frontend Environment Variables
# Copy this file to .env.local and update values as needed

# API Base URL (without /api suffix - it will be added automatically)
# For local development with Docker backend:
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Legacy support (will be used if NEXT_PUBLIC_API_BASE_URL is not set)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Database URL (if frontend needs direct DB access)
# DATABASE_URL=postgresql://user:password@localhost:5432/smartschedule
